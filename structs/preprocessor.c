#include <stdio.h>
#define SIZE 10    // Constant
#define iseven(n)  n % 2 == 0 ? 1 : 0

void main()
{

  int a[SIZE], i,v = 11, r;

        r = iseven(v);  //  v % 2 == 0 ? 1 : 0

        for(i = 0; i < SIZE; i ++)
        {
        }

        for(i = 0; i < SIZE; i ++)
        {
        }

}
